const { APP_VERSION } = require('./appVersion.constants')

const { STATUS_CODES } = require('./statusCode.constants')

const { PAYMENT_METHOD } = require('./user.constants')



module.exports = {
    APP_VERSION,
    STATUS_CODES, 
    PAYMENT_METHOD
}
