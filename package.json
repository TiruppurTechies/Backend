{
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.5.1"
  },
  "name": "backend",
  "description": "[ Documentation pending ]",
  "version": "1.0.0",
 "main": "src/server.js",
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx pm2 start --node-args=\"-r dotenv/config\" src/server.js --name backend-server --watch --no-daemon"

  },
  "author": "",
  "license": "ISC"
}
